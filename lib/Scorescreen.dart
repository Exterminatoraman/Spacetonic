import 'package:flutter/material.dart';
class scorescreen extends StatelessWidget {
  final double bmiscore;
  const scorescreen({Key? key,required this.bmiscore}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
